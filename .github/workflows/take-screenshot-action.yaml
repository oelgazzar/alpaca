name: Take Screenshot Action

on:
  push:
    branches: [main]

jobs:
  take-screenshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: git config --local user.name "oelgazzar"
      - run: git config --local user.email "omelgazzar@gmail.com"
      - run: git commit -a -m "commit message"
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
            github_token: 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
            branch: main
            force_with_lease: true
